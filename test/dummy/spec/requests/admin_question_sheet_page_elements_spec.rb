require 'spec_helper'

describe "AdminQuestionSheetPageElementsPaths" do
  describe "GET /admin/question_sheet/elements" do
    xit "success" do
    	# need to provide
    	# question_sheet_id 
    	# pages_id
      # get qe.admin_question_sheet_page_elements_url
      # response.status.should be(200)
    end
  end
end
