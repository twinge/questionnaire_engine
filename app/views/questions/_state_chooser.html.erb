<%= select_tag "answers[#{state_chooser.id}]", 
    options_for_select([""] + state_chooser.choices, [state_chooser.response(@application).to_s]), 
    :class => 'select ' + state_chooser.validation_class, 
    :id => dom_id(state_chooser), :disabled => @application.frozen?&&@reference.nil?, :readonly => @application.frozen?&&@reference.nil? %>
