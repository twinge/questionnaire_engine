page.replace_html 'page-preview', { :partial => 'question_pages/question_page', :locals => { :page => @page } }
page.select('#list-pages li').each do |el|
  el.className = ''
end
# page.assign "activeElement", ""
