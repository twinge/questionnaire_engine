<%= form_for(@josh) do |f| %>
  <% if @josh.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@josh.errors.count, "error") %> prohibited this josh from being saved:</h2>

      <ul>
      <% @josh.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
