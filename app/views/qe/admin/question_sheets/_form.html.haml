
= form_for([:admin, @question_sheet]) do |f|
  - if @question_sheet.errors.any?
    #error_explanation
      %h2= "#{pluralize(@question_sheet.errors.count, "error")} prohibited this question_sheet from being saved:"
      %ul
        - @question_sheet.errors.full_messages.each do |msg|
          %li= msg
  
  = f.label "question sheet label"
  = f.text_field :label

  .actions
    = f.submit 'Save'
